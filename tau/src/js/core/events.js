/*global window, define */
(function (ns) {
	"use strict";
	//>>excludeStart("tauBuildExclude", pragmas.tauBuildExclude);
	define(
		[
			"../core"
		],
		function () {
			//>>excludeEnd("tauBuildExclude");
			/** @namespace ns.events */
			ns.events = {};
			//>>excludeStart("tauBuildExclude", pragmas.tauBuildExclude);
			return ns.events;
		}
	);
	//>>excludeEnd("tauBuildExclude");
}(ns));
